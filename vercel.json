{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "installCommand": "apt-get update && apt-get install -y python3 python3-distutils && npm install"
        }
      }
    ]
  }